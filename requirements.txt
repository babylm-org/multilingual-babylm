# Core dependencies
pandas>=1.5.0
numpy>=1.20.0,<2.0.0  # fasttext is incompatible with numpy >=2.0.0
tqdm>=4.65.0
requests>=2.28.0
python-dotenv>=0.20.0

# Text processing
ftfy>=6.1.0

# Language detection (for language filtering)
fasttext>=0.9.2

# HuggingFace dependencies
huggingface-hub>=0.16.0
datasets>=2.12.0
pyarrow>=11.0.0  # For parquet support


# Optional but recommended
lxml>=4.9.0  # Better XML parsing performance
transformers>=4.30.0  # For loading tokenizers

# Validate and process language codes
iso639-lang>=2.6.1

# Fetching multilngual resources
cloudscraper>=0.1.0
openai>=0.27.0
beautifulsoup4>=4.12.0  # For HTML parsing

# logging
loguru>=0.7.3